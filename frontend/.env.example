# Copy this file to .env and add your OpenStates API key.
# Do NOT commit the real .env to git.

# Get an API key from https://v3.openstates.org/
VITE_OPENSTATES_API_KEY=(your_openstates_api_key_here)
# If set to "true", the frontend will call the server proxy at /api/people.geo
# instead of calling the OpenStates API directly. This keeps your key on the server.
VITE_USE_SERVER_PROXY=true
